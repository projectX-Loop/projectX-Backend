spring:
  application:
    name: demo
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
ai-service:
  base-url: ${AI_SERVICE_BASE_URL:http://localhost:8000}
  calculate-timeout: 10s
